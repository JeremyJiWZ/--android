package wj.airwar;import android.content.pm.ActivityInfo;import android.os.Bundle;import android.support.v7.app.AppCompatActivity;import android.view.Menu;import android.view.MenuItem;import android.widget.ImageView;import android.widget.RelativeLayout;public class Double_Activity extends AppCompatActivity {    private ImageView box1,box2;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_double_);        setRequestedOrientation(ActivityInfo.SCREEN_ORIENTATION_LANDSCAPE);        box1 = (ImageView)findViewById(R.id.box1);        box2 = (ImageView)findViewById(R.id.box2);        box_init();    }    private void box_init(){        RelativeLayout.LayoutParams bp1 = new RelativeLayout.LayoutParams(GameActivity.length*11,GameActivity.length*11);        bp1.leftMargin = 0;        bp1.topMargin = GameActivity.length;        box1.setLayoutParams(bp1);        RelativeLayout.LayoutParams bp2 = new RelativeLayout.LayoutParams(GameActivity.length*11,GameActivity.length*11);        bp2.leftMargin = GameActivity.length*12;        bp2.topMargin = GameActivity.length;        box2.setLayoutParams(bp2);    }    @Override    public boolean onCreateOptionsMenu(Menu menu) {        // Inflate the menu; this adds items to the action bar if it is present.        getMenuInflater().inflate(R.menu.dou_act, menu);        return true;    }    @Override    public boolean onOptionsItemSelected(MenuItem item) {        // Handle action bar item clicks here. The action bar will        // automatically handle clicks on the Home/Up button, so long        // as you specify a parent activity in AndroidManifest.xml.        int id = item.getItemId();        //noinspection SimplifiableIfStatement        if (id == R.id.action_settings) {            return true;        }        return super.onOptionsItemSelected(item);    }}